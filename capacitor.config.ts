import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rankak.auranative',
  appName: 'Aura Mobile Native',
  webDir: 'dist'
};

export default config;
